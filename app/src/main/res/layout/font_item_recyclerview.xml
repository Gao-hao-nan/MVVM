<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="mode"
            type="com.ghn.cocknovel.viewmodel.BookStoreViewModel" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            android:id="@+id/font_name"
            android:text="王漢宗勘亭流繁"
            android:textSize="18sp"
            android:textColor="@color/hei"
            android:paddingStart="10dp"
            android:paddingVertical="10dp"
            />
        <TextView
            android:id="@+id/font_use"
            android:layout_width="50dp"
            android:layout_height="20dp"
            android:text="使用"
            android:gravity="center"
            android:textColor="#E82719"
            app:layout_constraintTop_toTopOf="@id/font_name"
            app:layout_constraintRight_toRightOf="parent"
            android:layout_marginEnd="15dp"
            android:layout_marginVertical="@dimen/dp_10"
            android:textSize="15sp"
            android:background="@drawable/background_text_font"
            />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
